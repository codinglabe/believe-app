# If browsers show "413 Request Entity Too Large" from nginx, the request never
# reaches Laravel. Add one of the following in the server { } block (or in
# http { } for all sites), then reload nginx: sudo nginx -s reload
#
#   client_max_body_size 12M;
#
# This app allows offer images up to 5MB; 12M leaves headroom for multipart overhead.
