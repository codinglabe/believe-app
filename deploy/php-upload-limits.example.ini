; Copy directives into the php.ini or PHP-FPM pool used by this site (e.g.
; /etc/php/8.x/fpm/pool.d/www.conf as php_admin_value[...]).
;
; CLI limits shown by `php -i` may differ from php-fpm — confirm with phpinfo()
; or `php-fpm -i` on the pool socket.

upload_max_filesize = 12M
post_max_size = 25M
